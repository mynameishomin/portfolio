type GuardTest = (arg: any) => boolean;
type GuardTests = GuardTest | GuardTest[];
type GuardTestGroups = GuardTests[];
export declare const generateGuard: <T>(...testGroups: GuardTestGroups) => (() => boolean) | ((maybe?: unknown) => maybe is T);
export declare const isGuardTest: (() => boolean) | ((maybe?: unknown) => maybe is GuardTest);
export declare const isGuardTests: (() => boolean) | ((maybe?: unknown) => maybe is GuardTests);
export declare const isGuardTestGroups: (() => boolean) | ((maybe?: unknown) => maybe is GuardTests);
export {};
